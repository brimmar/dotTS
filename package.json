{
  "name": "dotts",
  "version": "0.1.0",
  "description": "Declarative dotfiles management with TypeScript — like AWS CDK for your system",
  "type": "module",
  "main": "./dist/public.js",
  "types": "./dist/public.d.ts",
  "exports": {
    ".": {
      "types": "./dist/public.d.ts",
      "default": "./dist/public.js"
    }
  },
  "files": [
    "dist"
  ],
  "bin": {
    "dotts": "./src/index.ts"
  },
  "scripts": {
    "start": "bun src/index.ts",
    "test": "bun test",
    "test:coverage": "bun test --coverage",
    "lint": "biome lint --apply .",
    "format": "biome format --write .",
    "type-check": "tsc --noEmit",
    "build": "bun build ./src/index.ts --compile --outfile dotts",
    "build:npm": "rm -rf dist && bun run build:npm:types && bun run build:npm:js",
    "build:npm:types": "tsc --project tsconfig.npm.json",
    "build:npm:js": "bun build ./src/public.ts --outdir dist --target bun --format esm --external effect --external zod --external mustache --external picocolors --external '@clack/prompts'",
    "prepublishOnly": "bun run ci && bun run build:npm",
    "ci": "bun run test && bun run lint && bun run type-check && bun run build"
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/brimmar/dotTS.git"
  },
  "homepage": "https://github.com/brimmar/dotTS#readme",
  "bugs": {
    "url": "https://github.com/brimmar/dotTS/issues"
  },
  "publishConfig": {
    "access": "public"
  },
  "keywords": [
    "dotfiles",
    "dotts",
    "configuration",
    "declarative",
    "typescript",
    "iac",
    "infrastructure-as-code"
  ],
  "devDependencies": {
    "@types/bun": "^1.3.13",
    "@types/mustache": "^4.2.6",
    "@types/node": "^25.6.0",
    "biome": "^0.3.3"
  },
  "peerDependencies": {
    "typescript": "^6.0.3"
  },
  "dependencies": {
    "@clack/prompts": "^0.11.0",
    "effect": "^3.21.2",
    "mustache": "^4.2.0",
    "picocolors": "^1.1.1",
    "typescript": "^6.0.3",
    "zod": "^4.3.6"
  },
  "bunInstall": {
    "allowScripts": false
  }
}
